(lgGqRPJFTvUTLq(0x1ce),s$PhMiaiKfW$CXgqSo[lgGqRPJFTvUTLq(0x1f9)]));}for(const kieJsEIlhnQPS of gZXXTgrwSRoEGvi[lgGqRPJFTvUTLq(0x260)]()){if(typeof kieJsEIlhnQPS[lgGqRPJFTvUTLq(0x25a)]===lgGqRPJFTvUTLq(0x298))try{const IlbBi$fgbBvxa_pN=await kieJsEIlhnQPS[lgGqRPJFTvUTLq(0x25a)](sock,eqRMTRwIlKClTVJVjTKriT$bR);if(IlbBi$fgbBvxa_pN===!![])return;}catch(g_lzYJZ_Y){console[lgGqRPJFTvUTLq(0x2a3)](lgGqRPJFTvUTLq(0x21b)+kieJsEIlhnQPS[lgGqRPJFTvUTLq(0x24a)]+'):',g_lzYJZ_Y);}}if(eqRMTRwIlKClTVJVjTKriT$bR[lgGqRPJFTvUTLq(0x295)]&&eqRMTRwIlKClTVJVjTKriT$bR[lgGqRPJFTvUTLq(0x295)][lgGqRPJFTvUTLq(0x271)](global[lgGqRPJFTvUTLq(0x1d8)])){const EPgUYk$tD=eqRMTRwIlKClTVJVjTKriT$bR[lgGqRPJFTvUTLq(0x295)][lgGqRPJFTvUTLq(0x262)](global[lgGqRPJFTvUTLq(0x1d8)][lgGqRPJFTvUTLq(0x2ad)])[lgGqRPJFTvUTLq(0x1c4)]()[lgGqRPJFTvUTLq(0x24e)](/\s+/),yLqUiCyLWgVJBXtSQ=EPgUYk$tD[lgGqRPJFTvUTLq(0x24d)]()[lgGqRPJFTvUTLq(0x1e9)](),WL_MmZCvgdTGS=gZXXTgrwSRoEGvi[lgGqRPJFTvUTLq(0x20b)](yLqUiCyLWgVJBXtSQ);if(WL_MmZCvgdTGS)try{await WL_MmZCvgdTGS[lgGqRPJFTvUTLq(0x265)](sock,eqRMTRwIlKClTVJVjTKriT$bR,EPgUYk$tD);}catch(WtUymhRWgyZ_mYX$PS){console[lgGqRPJFTvUTLq(0x2a3)](lgGqRPJFTvUTLq(0x28a)+yLqUiCyLWgVJBXtSQ+'):',WtUymhRWgyZ_mYX$PS),await eqRMTRwIlKClTVJVjTKriT$bR[lgGqRPJFTvUTLq(0x1fa)](lgGqRPJFTvUTLq(0x2a9));}}}),sock['ev']['on'](QlK$xCRCJKrhBOwIyV(0x267),async BwHiwIpSTH$ZR=>{const TQKwibH__J=QlK$xCRCJKrhBOwIyV;try{if(!global[TQKwibH__J(0x251)]?.[TQKwibH__J(0x236)])return;const tCJdHJHxaCtSqxWQpJXcYZv=BwHiwIpSTH$ZR['id'];for(const RJwLeduvBpD$xIKHWXqZX of BwHiwIpSTH$ZR[TQKwibH__J(0x290)]){const fsKdTbWOlK$LUBg=typeof RJwLeduvBpD$xIKHWXqZX===TQKwibH__J(0x237)?RJwLeduvBpD$xIKHWXqZX:RJwLeduvBpD$xIKHWXqZX[TQKwibH__J(0x1f6)]||RJwLeduvBpD$xIKHWXqZX['id'];if(!fsKdTbWOlK$LUBg)continue;const m_PjAI=fsKdTbWOlK$LUBg[TQKwibH__J(0x24e)]('@')[-parseInt(0x1)*parseInt(parseInt(0x1f26))+-parseInt(0x1)*0x1951+-0x1*Math.floor(-parseInt(0x3877))];if(BwHiwIpSTH$ZR[TQKwibH__J(0x1d5)]===TQKwibH__J(0x2b5)){if(fsKdTbWOlK$LUBg===sock[TQKwibH__J(0x250)]['id'])continue;const XKjJDXS$BsZ=TQKwibH__J(0x289)+m_PjAI+TQKwibH__J(0x2b0);await sock[TQKwibH__J(0x29b)](tCJdHJHxaCtSqxWQpJXcYZv,{'text':XKjJDXS$BsZ,'mentions':[fsKdTbWOlK$LUBg]});}else{if(BwHiwIpSTH$ZR[TQKwibH__J(0x1d5)]===TQKwibH__J(0x28e)){const tagVw_DZznJX=TQKwibH__J(0x1d3)+m_PjAI+TQKwibH__J(0x243);await sock[TQKwibH__J(0x29b)](tCJdHJHxaCtSqxWQpJXcYZv,{'text':tagVw_DZznJX,'mentions':[fsKdTbWOlK$LUBg]});}}}}catch(fDi_uqKFEWHessbsTIKkS){console[TQKwibH__J(0x2a3)](TQKwibH__J(0x214),fDi_uqKFEWHessbsTIKkS);}}),sock['ev']['on'](QlK$xCRCJKrhBOwIyV(0x1d0),async PBcTVCL_dTC=>{const TuCgfcGHAs_bbCvVQOkyk$Ihli=QlK$xCRCJKrhBOwIyV;console[TuCgfcGHAs_bbCvVQOkyk$Ihli(0x1ca)](TuCgfcGHAs_bbCvVQOkyk$Ihli(0x1f2),PBcTVCL_dTC);});}catch(GhqAWfuTNInSJw){console[QlK$xCRCJKrhBOwIyV(0x2a3)](QlK$xCRCJKrhBOwIyV(0x226),GhqAWfuTNInSJw),isConnecting=![],setTimeout(()=>startBot(),parseInt(0x3b8f)+Math.max(-parseInt(0x4de),-parseInt(0x4de))*parseInt(-0x1)+-0x195d);}})());}function collectRequestBody(qjUYIyrzQqS$TSgN){return new Promise((QT$HqEOSSKZLaua_FYs,XkrUlsJxO_ZSc$npOrmAybN)=>{const OyxDLcMcMAO_fSMJUsPRNjU=STmbsXjYYXkGnoIz;let HG_Hh_KdJPhfsozbwEIWLKzD='';qjUYIyrzQqS$TSgN['on'](OyxDLcMcMAO_fSMJUsPRNjU(0x1c9),o$YpcjToWk=>{const Yn$FmgrSHwSR=OyxDLcMcMAO_fSMJUsPRNjU;HG_Hh_KdJPhfsozbwEIWLKzD+=o$YpcjToWk,HG_Hh_KdJPhfsozbwEIWLKzD[Yn$FmgrSHwSR(0x2ad)]>Math.max(-0x23d1,-0x23d1)*-parseInt(0x5)+parseInt(0x1e)*-parseInt(0x5d4a)+0x197dd7&&(qjUYIyrzQqS$TSgN[Yn$FmgrSHwSR(0x1e0)](),XkrUlsJxO_ZSc$npOrmAybN(new Error(Yn$FmgrSHwSR(0x1c6))));}),qjUYIyrzQqS$TSgN['on'](OyxDLcMcMAO_fSMJUsPRNjU(0x1c5),()=>QT$HqEOSSKZLaua_FYs(HG_Hh_KdJPhfsozbwEIWLKzD)),qjUYIyrzQqS$TSgN['on'](OyxDLcMcMAO_fSMJUsPRNjU(0x2a3),XkrUlsJxO_ZSc$npOrmAybN);});}const server=http[TSBbB_tCBGfsKBRrPbRq(0x213)](async(PseCXOfvXEHiLWTypVxQjU,b$QkSy)=>{const ZVHO_cTRkCzxJVi$jSJKWTEPn=TSBbB_tCBGfsKBRrPbRq,poiilsOqtOTZ$GH_B=PseCXOfvXEHiLWTypVxQjU[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x2ab)];if(poiilsOqtOTZ$GH_B==='/'||poiilsOqtOTZ$GH_B===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x29d))b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](Math.ceil(parseInt(0x1c40))+-parseInt(0x85)*Math.floor(-parseInt(0x2))+-parseInt(0x1c82),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x248)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x27f));else{if(poiilsOqtOTZ$GH_B===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1df)&&PseCXOfvXEHiLWTypVxQjU[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x23a)]===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x20d))b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](0x2*parseInt(0x11f1)+Math.trunc(-0x703)+Math.floor(-parseInt(0x1c17)),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x200)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](JSON[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x27c)]({'status':botStatus,'connecting':isConnecting}));else{if(poiilsOqtOTZ$GH_B===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x296)&&PseCXOfvXEHiLWTypVxQjU[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x23a)]===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x20d))b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](parseInt(0x2f)*Math.trunc(-parseInt(0x59))+parseInt(0x100a)+parseInt(0x115),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1e8)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x20c));else{if(poiilsOqtOTZ$GH_B===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x296)&&PseCXOfvXEHiLWTypVxQjU[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x23a)]===ZVHO_cTRkCzxJVi$jSJKWTEPn(0x23f))try{const vtIW_HlsBCraK=await collectRequestBody(PseCXOfvXEHiLWTypVxQjU),adAyYkVLkBp_tJyvyamw=new URLSearchParams(vtIW_HlsBCraK),XcgpEdGwoWoLNtOxMUVDtB=(adAyYkVLkBp_tJyvyamw[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x20b)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x223))||'')[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x26e)](/[^0-9]/g,'');if(!XcgpEdGwoWoLNtOxMUVDtB)return b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](-0x1b96+Number(0x3a)*Math.floor(-parseInt(0x3d))+0x2c*parseFloat(parseInt(0xfa)),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x24f)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x227));if(!sock)return b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](0x915+0x2233+-parseInt(0x7)*Math.floor(0x5e7),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x24f)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x201));const oFJCZqoBif=await sock[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x264)](XcgpEdGwoWoLNtOxMUVDtB);pairingCodes[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x2ba)](XcgpEdGwoWoLNtOxMUVDtB,oFJCZqoBif),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](Number(parseInt(0x716))*-0x2+-0xa*-parseInt(0x161)+-parseInt(0x1)*-parseInt(0x12a),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1e8)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x261)+oFJCZqoBif+ZVHO_cTRkCzxJVi$jSJKWTEPn(0x29a));}catch(aKfgcMgWXKlyYaBsuHo_OUnF){console[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x2a3)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1d7),aKfgcMgWXKlyYaBsuHo_OUnF[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1f9)]),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](0x126*-0x1f+parseFloat(-parseInt(0xf))*-0x1a5+0x1*0xce3,{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x24f)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1fb)+aKfgcMgWXKlyYaBsuHo_OUnF[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1f9)]);}else b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x21d)](-parseInt(0x1)*parseInt(parseInt(0x1c2f))+Math.ceil(-parseInt(0x923))*0x1+-0x1373*-parseInt(0x2),{'Content-Type':ZVHO_cTRkCzxJVi$jSJKWTEPn(0x24f)}),b$QkSy[ZVHO_cTRkCzxJVi$jSJKWTEPn(0x1c5)](ZVHO_cTRkCzxJVi$jSJKWTEPn(0x218));}}}});server[TSBbB_tCBGfsKBRrPbRq(0x2a7)](PORT,()=>{const PxujmcCHstQLR=TSBbB_tCBGfsKBRrPbRq;console[PxujmcCHstQLR(0x1ca)](PxujmcCHstQLR(0x2ac)+PORT);}),global[TSBbB_tCBGfsKBRrPbRq(0x27b)]=server,global[TSBbB_tCBGfsKBRrPbRq(0x1ec)]=PORT,loadPrefix(),process['on'](TSBbB_tCBGfsKBRrPbRq(0x277),iBhnLtUpwX$mW=>{const tpUoVYA_NsiZeoreHbtK=TSBbB_tCBGfsKBRrPbRq;console[tpUoVYA_NsiZeoreHbtK(0x2a3)](tpUoVYA_NsiZeoreHbtK(0x25e),iBhnLtUpwX$mW);}),process['on'](TSBbB_tCBGfsKBRrPbRq(0x20a),B$qaBZKXDNBgbmzP=>{const PqjZyzuywHbS$TGvzUhPS=TSBbB_tCBGfsKBRrPbRq;console[PqjZyzuywHbS$TGvzUhPS(0x2a3)](PqjZyzuywHbS$TGvzUhPS(0x266),B$qaBZKXDNBgbmzP);});


require('./config')
const { default: makeWASocket, useMultiFileAuthState, DisconnectReason, downloadMediaMessage, generateWAMessageContent, generateWAMessageFromContent, generateMessageID, prepareWAMessageMedia, fetchLatestWaWebVersion, proto,generateProfilePicture, getContentType } = require('@whiskeysockets/baileys');
const pino = require('pino');
const fs = require('fs');
const path = require('path');
const http = require('http');
const QRCode = require('qrcode');
const { Boom } = require('@hapi/boom');
const { sendButtons, sendInteractiveMessage } = require('gifted-btns');
const serializeMessage = require('./handler.js');
const { decodeSessionId } = require('./lib/sessionLoader');
const { AntideleteHandler } = require('./lib/antidelete');
const { handleChatbotResponse } = require('./lib/chatbot');
const { handleLinkDetection } = require('./lib/antilink');
const JimpImport = require('jimp');

const Jimp =
  JimpImport.read
    ? JimpImport
    : JimpImport.Jimp
    ? JimpImport.Jimp
    : JimpImport.default;

global.generateWAMessageContent = generateWAMessageContent;
global.generateWAMessageFromContent = generateWAMessageFromContent;
global.generateMessageID = generateMessageID;
global.prepareWAMessageMedia = prepareWAMessageMedia;
global.proto = proto;
global.Jimp = Jimp;
global.generateProfilePicture = generateProfilePicture;
global.downloadMediaMessage = downloadMediaMessage;
global.bannedChats = global.bannedChats || [];
if (!fs.existsSync(__dirname + '/session/creds.json') && global.sessionid) {
    const result = decodeSessionId(global.sessionid);
    if (result.ok) {
        try {
            fs.mkdirSync(__dirname + '/session', { recursive: true });
            fs.writeFileSync(__dirname + '/session/creds.json', result.data);
            console.log('✅ Session restored from SESSION_ID');
        } catch (err) {
            console.error('Error writing restored session:', err.message);
        }
    } else {
        console.error('❌ Failed to restore session from SESSION_ID:', result.reason);
        console.warn('⚠️ Make sure you copied the FULL session string (e.g. POPKID~...).');
    }
}

const AUTH_FOLDER = './session';
const PLUGIN_FOLDER = './plugins';
const PORT = process.env.PORT || 3000;

let latestQR = '';
let botStatus = 'disconnected';
let pairingCodes = new Map();
let presenceInterval = null;
let sock = null;
let isConnecting = false;
let lastStatusReactTime = 0;

// --- Status-reaction LID resolution -----------------------------------
// WhatsApp sometimes reports a status poster as an @lid (linked-device id)
// instead of their real @s.whatsapp.net JID. Reacting to a status with the
// wrong JID in `key.participant` fails silently, which is why "auto like
// status" can look broken even though the code runs without errors.
// This cache + resolver mirrors the approach used by Toxic-MD: cache any
// LID→phone mapping we learn, and try several real resolution paths
// (Baileys' own lid-mapping store first) before falling back to whatever
// Baileys handed us.
const lidPhoneCache = new Map();

function cacheLidPhone(lidNum, phoneNum) {
    if (!lidNum || !phoneNum || lidNum === phoneNum) return;
    lidPhoneCache.set(lidNum, phoneNum);
}

async function resolveStatusParticipant(sock, rawMsg) {
    const rawParticipant = rawMsg.key.participant;
    if (!rawParticipant || !rawParticipant.endsWith('@lid')) {
        return rawParticipant;
    }

    const lidNum = rawParticipant.split('@')[0].split(':')[0];

    // 1) Already resolved this LID before in this session.
    const cached = lidPhoneCache.get(lidNum);
    if (cached) {
        return `${cached}@s.whatsapp.net`;
    }

    // 2) Baileys sometimes attaches the real number directly on the event.
    const rawPn = rawMsg.key?.participantPn || rawMsg.key?.senderPn || rawMsg.participantPn;
    if (rawPn) {
        const resolved = rawPn.includes('@') ? rawPn : `${rawPn}@s.whatsapp.net`;
        const phoneNum = resolved.split('@')[0].split(':')[0];
        cacheLidPhone(lidNum, phoneNum);
        return resolved;
    }

    // 3) Ask Baileys' own LID↔PN mapping store, trying a couple of formats
    //    since the stored key isn't always exactly what we received.
    if (sock.signalRepository?.lidMapping?.getPNForLID) {
        const variants = [rawParticipant, `${lidNum}:0@lid`, `${lidNum}@lid`];
        for (const variant of variants) {
            try {
                const pn = await sock.signalRepository.lidMapping.getPNForLID(variant);
                if (pn && typeof pn === 'string') {
                    const phoneNum = pn.split('@')[0].split(':')[0].replace(/[^0-9]/g, '');
                    if (phoneNum.length >= 7 && phoneNum !== lidNum) {
                        cacheLidPhone(lidNum, phoneNum);
                        return `${phoneNum}@s.whatsapp.net`;
                    }
                }
            } catch (err) {
                // try the next variant
            }
        }
    }

    // 4) Legacy fallback some Baileys builds exposed.
    if (typeof sock.getJidFromLid === 'function') {
        const resolved = await sock.getJidFromLid(rawParticipant).catch(() => null);
        if (resolved) {
            const phoneNum = resolved.split('@')[0].split(':')[0];
            cacheLidPhone(lidNum, phoneNum);
            return resolved;
        }
    }

    // Nothing worked — return the raw @lid, same as before, so the caller
    // keeps its existing "best effort" behavior instead of crashing.
    return rawParticipant;
}
// -----------------------------------------------------------------------

function loadPrefix() {
    const configPath = path.join(__dirname, 'config.json');
    if (fs.existsSync(configPath)) {
        try {
            const config = JSON.parse(fs.readFileSync(configPath, 'utf8'));
            if (config.prefix) {
                global.BOT_PREFIX = config.prefix;
                console.log(`✅ Loaded prefix: ${global.BOT_PREFIX}`);
            }
        } catch (err) {
            console.error('Error loading config:', err);
        }
    }
    startBot();
}

function startBot() {
    console.log('🚀 Starting WhatsApp Bot...');
    isConnecting = true;

    if (!fs.existsSync(AUTH_FOLDER)) {
        fs.mkdirSync(AUTH_FOLDER, { recursive: true });
    }

    const credsPath = path.join(AUTH_FOLDER, 'creds.json');
    if (fs.existsSync(credsPath)) {
        try {
            const creds = JSON.parse(fs.readFileSync(credsPath, 'utf8'));
            if (creds.noiseKey && creds.noiseKey.private) {

                console.log('📁 Using existing session...');
            } else {
                console.log('⚠️ Invalid session detected, will create new one...');
            }
        } catch (err) {
            console.log('⚠️ Corrupted session, will create new one...');
        }
    }

    (async () => {
        try {
            const { version, isLatest } = await fetchLatestWaWebVersion();
            console.log(`📱 Using WA v${version.join(".")}, isLatest: ${isLatest}`);

            const { state, saveCreds } = await useMultiFileAuthState(AUTH_FOLDER);

            sock = makeWASocket({
                version,
                logger: pino({ level: 'silent' }),
                auth: state,
                printQRInTerminal: true,
                keepAliveIntervalMs: 10000,
                markOnlineOnConnect: true,
                syncFullHistory: false,
                browser: ['Bot', 'Chrome', '1.0.0']
            });

            sock.ev.on('connection.update', async (update) => {
                const { connection, lastDisconnect, qr } = update;

                if (qr) {
                    QRCode.toDataURL(qr, (err, url) => {
                        if (!err) {
                            latestQR = url;
                        }
                    });
                }

                if (connection === 'close') {
                    botStatus = 'disconnected';
                    isConnecting = false;

                    if (presenceInterval) {
                        clearInterval(presenceInterval);
                        presenceInterval = null;
                    }

                    const statusCode = (lastDisconnect?.error instanceof Boom)
                        ? lastDisconnect.error.output.statusCode
                        : 0;

                    const shouldReconnect = statusCode !== DisconnectReason.loggedOut;

                    if (shouldReconnect) {
                        setTimeout(() => startBot(), 5000);
                    } else {
                        if (fs.existsSync(AUTH_FOLDER)) {
                            fs.rmSync(AUTH_FOLDER, { recursive: true, force: true });
                        }
                        setTimeout(() => startBot(), 3000);
                    }
                }

                else if (connection === 'open') {
                    botStatus = 'connected';
                    isConnecting = false;

                    if (!global.owners) global.owners = [];
                    if (!global.owners.includes(sock.user.id)) {
                        global.owners.push(sock.user.id);
                    }

                    presenceInterval = setInterval(() => {
                        if (sock?.ws?.readyState === 1) {
                            sock.sendPresenceUpdate('available');
                        }
                    }, 10000);

                    // Small delay so the socket is fully ready before sending
                    await new Promise(resolve => setTimeout(resolve, 1500));

                    // Clean self-JID (strip :device suffix) so the DM actually lands
                    const botNumber = sock.user.id.split(':')[0] + '@s.whatsapp.net';

                    try {
                        await sock.newsletterFollow('120363426778975572@newsletter');
                        console.log('📡 Auto-followed Official Newsletter');
                    } catch (err) {
                        console.log('Newsletter follow verified.');
                    }

                    try {
                        await sock.sendMessage(botNumber, {
                            text: `🤖 Bot Connected Successfully!\n\n⏰ Time: ${new Date().toLocaleString()}\n✅ Status: Online and Ready!\n📝 Prefix: ${global.BOT_PREFIX}\n👑 Owners: ${global.owners.length}\n\n✅ Make sure to join below channel`,
                            contextInfo: {
                                forwardingScore: 1,
                                isForwarded: true,
                                forwardedNewsletterMessageInfo: {
                                    newsletterJid: '120363426778975572@newsletter',
                                    newsletterName: 'Popkid',
                                    serverMessageId: -1
                                }
                            }
                        });
                    } catch (err) {
                        console.log('❌ Connection message error:', err.message);
                    }
                }

                else if (connection === 'connecting') {
                    botStatus = 'connecting';
                    isConnecting = true;
                }
            });

            sock.ev.on('creds.update', async () => {
                await saveCreds();
                console.log('💾 Credentials updated');
            });

            // global.plugins is shared with arslan.js's cmd() registry, so a
            // command-style plugin (const { cmd } = require('../arslan')) and a
            // legacy { name, execute } plugin land in the exact same Map.
            global.plugins = global.plugins instanceof Map ? global.plugins : new Map();
            const plugins = global.plugins;
            const pluginPath = path.join(__dirname, PLUGIN_FOLDER);

            if (fs.existsSync(pluginPath)) {
                try {
                    const pluginFiles = fs.readdirSync(pluginPath).filter(file => file.endsWith('.js'));

                    for (const file of pluginFiles) {
                        try {
                            const beforeCount = plugins.size;
                            const plugin = require(path.join(pluginPath, file));

                            if (plugins.size > beforeCount) {
                                // Self-registered one or more commands via cmd() in ./arslan.js
                                console.log(`✅ Loaded command plugin: ${file}`);
                            } else if (plugin && plugin.name && typeof plugin.execute === 'function') {
                                plugins.set(plugin.name.toLowerCase(), plugin);
                                if (Array.isArray(plugin.aliases)) {
                                    plugin.aliases.forEach(alias => {
                                        plugins.set(alias.toLowerCase(), plugin);
                                    });
                                }
                                console.log(`✅ Loaded plugin: ${plugin.name}`);
                            } else {
                                console.warn(`⚠️ Invalid plugin structure in ${file}`);
                            }
                        } catch (error) {
                            console.error(`❌ Failed to load plugin ${file}:`, error.message);
                        }
                    }
                    console.log(`📦 Total plugins loaded: ${plugins.size}`);
                    global.plugins = plugins;
                } catch (error) {
                    console.error('❌ Error loading plugins:', error);
                }
            } else {
                console.log('📁 No plugins folder found');
            }

            sock.ev.on('messages.upsert', async ({ messages, type }) => {
                if (type !== 'notify' && type !== 'append') return;

                const CHANNEL_ID = "120363426778975572@newsletter";

                for (const rawMsg of messages) {
                    if (rawMsg.key?.remoteJid === CHANNEL_ID && rawMsg.key?.server_id) {
                        const emojis = ["❤️", "💛", "👍", "💜", "😮", "🤍", "💙", "🔥", "💯", "⚡"];
                        const emoji = emojis[Math.floor(Math.random() * emojis.length)];

                        try {
                            await sock.newsletterReactMessage(
                                CHANNEL_ID,
                                rawMsg.key.server_id.toString(),
                                emoji
                            );
                            console.log(`✅ Channel reaction: ${emoji} to message ${rawMsg.key.server_id}`);
                        } catch (err) {
                            console.log("❌ Channel React Error:", err.message);
                        }
                        continue;
                    }
                }

                for (const rawMsg of messages) {
                    if (rawMsg.key.remoteJid === 'status@broadcast' && rawMsg.key.participant) {
                        if (global.autoView) {
                            try {
                                console.log(`📱 Status detected from: ${rawMsg.key.participant}`);
                                await sock.readMessages([rawMsg.key]);
                            } catch (err) {
                                console.log('❌ Status viewer error:', err.message);
                            }
                        }

                        if (global.autoLike) {
                            try {
                                const now = Date.now();
                                if (now - lastStatusReactTime < (global.statusReactThrottleMs ?? 5000)) {
                                    // Throttle: skip if we reacted too recently
                                } else {
                                    // Resolve the poster's real JID — WhatsApp sometimes reports
                                    // this as an @lid (linked-device id) instead of @s.whatsapp.net,
                                    // and status reactions silently fail if sent to an @lid.
                                    const realJid = await resolveStatusParticipant(sock, rawMsg);

                                    const resolvedKey = {
                                        remoteJid: 'status@broadcast',
                                        id: rawMsg.key.id,
                                        participant: realJid
                                    };

                                    const contentType = getContentType(rawMsg.message);
                                    const reactable = ['imageMessage', 'videoMessage', 'extendedTextMessage', 'conversation', 'audioMessage'];

                                    if (reactable.includes(contentType)) {
                                        const emojis = ["❤️", "🩶", "🔥", "🤍", "♦️", "🎉", "💚", "💯", "✨", "😍", "🎊"];
                                        const emoji = emojis[Math.floor(Math.random() * emojis.length)];
                                        const botId = sock.user?.id ? sock.user.id.split(':')[0] + '@s.whatsapp.net' : sock.user?.id;

                                        await sock.sendMessage('status@broadcast',
                                            { react: { text: emoji, key: resolvedKey } },
                                            { statusJidList: [realJid, botId].filter(Boolean) }
                                        );

                                        lastStatusReactTime = Date.now();
                                        await new Promise(resolve => setTimeout(resolve, global.statusReactDelayMs ?? 2000));
                                    }
                                }
                            } catch (err) {
                                console.log('❌ Status like error:', err.message);
                            }
                        }

                        continue;
                    }
                }

                for (const rm of messages) {
                    AntideleteHandler(sock, rm).catch(err => console.error('Antidelete hook error:', err.message));
                }

                const rawMsg = messages[0];
                if (!rawMsg.message) return;

                const m = await serializeMessage(sock, rawMsg);

                if (global.autoRead) {
                    try { await sock.readMessages([rawMsg.key]); } catch (err) {}
                }

                if (global.presenceMode && global.presenceMode !== 'none' && m.from) {
                    try {
                        if (global.presenceMode === 'typing') await sock.sendPresenceUpdate('composing', m.from);
                        else if (global.presenceMode === 'recording') await sock.sendPresenceUpdate('recording', m.from);
                        else if (global.presenceMode === 'online') await sock.sendPresenceUpdate('available', m.from);
                    } catch (err) {}
                }

                if (m.isGroup && !rawMsg.key.fromMe) {
                    handleChatbotResponse(sock, m.from, rawMsg, m.body || '', m.sender)
                        .catch(err => console.error('Chatbot hook error:', err.message));

                    const isExempt = m.isAdmin || m.isOwner || m.isDev;
                    handleLinkDetection(sock, m.from, rawMsg, m.body || '', m.sender, isExempt)
                        .catch(err => console.error('Antilink hook error:', err.message));
                }

                for (const plugin of plugins.values()) {
                    if (typeof plugin.onMessage === 'function') {
                        try {
                            const blocked = await plugin.onMessage(sock, m);
                            if (blocked === true) return;
                        } catch (err) {
                            console.error(`❌ onMessage error (${plugin.name}):`, err);
                        }
                    }
                }

                if (m.bo
